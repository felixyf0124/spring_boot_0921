1. Delete the project from your workspace (without deleting the files on disk).
2. The Spring Boot project was moved to the /project sub-directory of the Git repo.
3. Import the /project/pom.xml file in your workspace.
4. You will find the files you need to complete the labs in the /files sub directory. Copy these files in the appropriate packages of the project.
